<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/itemContainer"
    android:layout_width="match_parent"
    android:layout_height="@dimen/tarjeta_height"
    android:layout_marginStart="@dimen/view_padding"
    android:layout_marginTop="@dimen/view_padding"
    android:layout_marginEnd="@dimen/view_padding"
    android:background="@drawable/tarjeta"
    android:padding="@dimen/tarjeta_padding"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toTopOf="parent">


    <ImageView
        android:id="@+id/station_image"
        android:layout_width="@dimen/image_width"
        android:layout_height="@dimen/image_height"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/stationname_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/spacebetweenviews_info"
        android:textAllCaps="true"
        android:textColor="@android:color/black"
        app:layout_constraintStart_toEndOf="@id/station_image"
        app:layout_constraintTop_toTopOf="@id/station_image" />

    <TextView
        android:id="@+id/loc_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/orange"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@id/station_image"
        app:layout_constraintStart_toStartOf="@id/stationname_textview"
        app:layout_constraintTop_toBottomOf="@id/stationname_textview" />

</androidx.constraintlayout.widget.ConstraintLayout>