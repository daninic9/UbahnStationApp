query GetStationInfo($id:Int!) {
    stationWithEvaId(evaId: $id) {
        name
        location {
            latitude
            longitude
        }
        picture {
            url
        }
    }
}